# Python
__pycache__/
*.pyc
*.pyo
.venv/

# Secrets
.env
.env.local
.env.*.local

# Spider runtime data
cache/
data/

# Repo-root corpus embeddings (large); keep out of git. The chatbot
# ships build artifacts under chatbot/corpus/, including tracked
# .embeddings.json for Vercel hybrid retrieval (see README).
corpus/.embeddings.json

# Node / Next.js
node_modules/
.next/
out/
*.log
npm-debug.log*

# Vercel
.vercel

# OS
.DS_Store
Thumbs.db

# Claude Code per-machine overrides (launch.json is shared)
.claude/settings.local.json

# TypeScript incremental build cache
**/tsconfig.tsbuildinfo
